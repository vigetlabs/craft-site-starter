{# @var \craft\elements\Entry|null block #}
{% set block = block ?? null %}

{{ include('_components/call-to-action.twig', {
  title: block.title,
  text: block.richTextSimple,
  href: block.href,
  linkText:  block.linkText ?? block.href.label ?? null,
  newWindow: block.newWindow,
}) }}
